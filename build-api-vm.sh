#!/bin/bash

apt-get update
# apt-get install apache2
# apt-get install libapache2-mod-wsgi-py3 python-dev

# Change VM's API's configuration to use the shared folder.

# then need something here to initalise flask
# so cd in and run flask app 